{
  "name": "my-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently -k -n backend,frontend -c yellow,cyan \"npm --prefix backend run dev\" \"npm --prefix frontend start\"",
    "start:dev": "npm run start",
    "start:prod": "npm --prefix backend run start",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "npm --prefix frontend run build",
    "build:backend": "npm --prefix backend run build --if-present",
    "install:all": "npm ci --prefix frontend && npm ci --prefix backend",
    "vercel-build": "npm ci --prefix frontend && npm run build --prefix frontend"
  },
  "devDependencies": {
    "@babel/preset-typescript": "^7.27.1",
    "@types/react": "^19.1.11",
    "@types/react-dom": "^19.1.7",
    "concurrently": "^9.2.0",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^5.2.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "react-router-dom": "^7.13.0"
  }
}
